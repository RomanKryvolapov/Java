In this code, I tried to turn the image into a character set, for each of the characters I calculated the relative brightness and converted each pixel to a character according to this calculation

В этом коде я попытался превратить изоражение в набор символов, для каждого из символов я рассчитал относительную яркость и преобразовал каждый пиксель в символ в соответствии с этим рассчетом